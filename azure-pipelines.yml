# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:
  - script: sudo apt-get install -y ruby
    displayName: 'Install Ruby'

  - script: sudo gem install bundler
    displayName: 'Install Bundler'

  - script: sudo gem install bundler
    displayName: 'Install Bundler'

  - script: sudo gem install nokogiri -- --use-system-libraries
    displayName: 'Install Nokogiri '

  - script: bundle install
    displayName: 'Install all of the required gems'

  - script: bundle exec rake book:build
    displayName: 'Generate the books'